import { CustomTabs } from 'react-native-custom-tabs';

export function test(url, opts = {}) {
  // TODO: fix (somehow CustomTabs.openUrl is undefined here)
  // return CustomTabs.openUrl(url, opts);
}
